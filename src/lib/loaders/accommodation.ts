// src/lib/loaders/accommodation.ts
import type { StayOption } from '../types';

export async function loadAccommodationOptions(): Promise<StayOption[]> {
  // This is temporary - you'll replace with actual data loading
  return [];
}